apiVersion: configmanagement.gke.io/v1
kind: ConfigManagement
metadata:
  name: config-management
spec:
  clusterName: prod2
  git:
    syncRepo: https://github.com/cgrant/cluster_config
    syncBranch: master
    secretType: none
    policyDir: "sample"